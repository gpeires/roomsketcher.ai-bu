services:
  cv-service:
    build: .
    ports:
      - "8100:8100"
    volumes:
      - ./fixtures:/app/fixtures
    environment:
      - LOG_LEVEL=debug
      - REPLICATE_API_TOKEN=${REPLICATE_API_TOKEN}
